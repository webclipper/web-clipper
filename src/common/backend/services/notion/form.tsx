import React from 'react';
import { FormattedMessage } from 'react-intl';

export default () => (
  <div style={{ textAlign: 'right' }}>
    <FormattedMessage
      id="backend.services.notion.form.notice"
      defaultMessage="Repository not Support Database"
    />
  </div>
);
